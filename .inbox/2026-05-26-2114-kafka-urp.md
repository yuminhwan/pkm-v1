---
date: 2026-05-26-2114
type: raw
source-url: ""
title: "Kafka Under Replicated Partitions (URP) 개념 및 대처 방안"
tags: [kafka, urp, 메시징]
---

# Kafka Under Replicated Partitions (URP)

## 1. 핵심 개념

### ISR (In-Sync Replicas)
- 파티션의 **현재 동기 상태**에 있는 복제본 집합
- 리더(Leader)와 일정 수준(**replica.lag.time.max.ms**) 내로 데이터를 따라잡은 팔로워만 ISR에 포함
- `min.insync.replicas` 이상이 ISR에 있어야 `acks=all` producers가 정상 동작

### Under-Replicated Partition (URP)
- **특정 파티션의 ISR 수가 전체 복제본 수보다 적은 상태**
- 예: replication factor=3인데 ISR에 2개만 남으면 해당 파티션이 under-replicated
- `kafka-topics.sh --describe` 출력에서 `Isr` 값이 `Replicas` 값보다 작으면 URP

---

## 2. URP 발생 원인

| 원인 | 설명 |
|------|------|
| **브로커 다운/비정상** | 브로커 장애 시 해당 브로커의 복제본이 ISR에서 제외 |
| **네트워크 지연** | 브로커 간 네트워크 레이턴시 증가로 팔로워가 리더를 못 따라감 |
| **디스크 I/O 병목** | 팔로워 브로커의 디스크 쓰기 지연 |
| **CPU 과부하** | 팔로워 브로커 리소스 부족 |
| **대규모 로드 밸런싱/재시작** | 롤링 업데이트 시 일시적 URP |
| **replica.lag.time.max.ms 초과** | 팔로워가 이 시간 내에 fetch하지 못하면 ISR에서 제거 |

---

## 3. Replication Lag

- **리더와 팔로워 간의 오프셋 차이** 또는 시간 기반 지연
- `replica.lag.time.max.ms`가 핵심 튜닝 파라미터 (기본값: 30초)
- lag가 이 임계값을 초과하면 팔로워는 ISR에서 **자동 제거**

---

## 4. 모니터링 방법

### JMX Metrics (핵심)
```
# URP 파티션 수
kafka.server:type=ReplicaManager,name=UnderReplicatedPartitions

# ISR 삭제/추가 횟수
kafka.server:type=ReplicaManager,name=IsrShrinksPerSec
kafka.server:type=ReplicaManager,name=IsrExpandsPerSec

# 리더选举 횟수
kafka.controller:type=KafkaController,name=LeaderElectionRateAndTimeMs

# 복제 레이턴시
kafka.server:type=ReplicaFetcherManager,name=MaxLag
kafka.server:type=ReplicaFetcherManager,name=ReplicationLag
```

### CLI 모니터링
```bash
# 전체 토픽 URP 확인
kafka-topics.sh --bootstrap-server localhost:9092 --describe --under-replicated-partitions

# 특정 브로커의 파티션 상태
kafka-leader-election.sh --bootstrap-server localhost:9092 --describe
```

### 시스템 도구
- **Prometheus + Grafana**: `kafka-exporter` 또는 `Burrow` 활용
- **Confluent Control Center** / **Cruise Control**: 상업적/오픈소스 모니터링
- **Burrow**: 컨슈머 레이지 모니터링 특화

---

## 5. 대처 방안

### 즉시 대응
1. **장애 브로커 확인** → 로그 및 시스템 상태 점검
2. `UnderReplicatedPartitions`가 0이 아니면 즉시 알림 발동
3. **불필요한 리더 선거 억제**: `auto.leader.rebalance.enable=false` (긴급 시)

### 장기적 개선
- **replica.lag.time.max.ms 튜닝**: 네트워크 환경에 맞게 조정
- **min.insync.replicas 설정**: `acks=all` 사용 시 최소 2 이상 권장
- **hardware 업그레이드**: 디스크 I/O, 네트워크 대역폭 확보
- **rack awareness 설정**: 가용 영역 분산으로 동시 장애 가능성 감소
- **Cruise Control**로 자동화된 리밸런싱 구현

### 알림 임계값 예시
```
UnderReplicatedPartitions > 0  → Warning (1분 이상 지속 시)
UnderReplicatedPartitions > 0  → Critical (5분 이상 지속 시)
IsrShrinksPerSec > 0          → Warning
```

---

## 요약

**URP = 정상 동기화되지 않은 복제본 존재**를 의미하며, 데이터 내구성과 가용성에 직접 영향. JMX 메트릭과 `kafka-topics --describe --under-replicated-partitions`를 통해 지속 모니터링하고, 네트워크/디스크/브로커 안정성을 확보하는 것이 근본적 해결책입니다.
