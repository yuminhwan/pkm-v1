# PKM v1 — fork한 사람의 raw 자료는 push되지 않게
# .inbox/는 raw 던지는 자리 (스크린샷·녹취록·외부 글 등) — 본인 사적 자료
# outbox/는 본인이 직접 쓰는 글이므로 commit하고 싶을 수도, 아닐 수도 있음 — 본인이 결정

.inbox/*
!.inbox/README.md

# 일일회고가 webhook 없을 때 .inbox/에 저장하는 결과물도 자동으로 제외됨

# OS·에디터
.DS_Store
*.swp
.idea/
.vscode/
